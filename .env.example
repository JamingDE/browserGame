# Client → Server (lokal). In Produktion automatisch gleiches Origin.
VITE_API_BASE=

# Server-Port (Render setzt PORT automatisch)
PORT=3001

# Optional für Bildsuche (M4). Bis M4 leer lassen, App läuft trotzdem.
PIXABAY_API_KEY=
UNSPLASH_ACCESS_KEY=
